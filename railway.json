{
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "npm run build && npx ts-node src/bot/deploy-commands.ts && npm start",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}
